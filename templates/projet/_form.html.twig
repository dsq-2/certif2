{# {{ form_start(form) }}
    {{ form_widget(form) }}
    


{{ form_end(form) }} #}


<div class="container">
{{ form_start(form) }}
	<div class="card border-light mt-5 shadow-sm p-3" >
		<div class="card-header"><h1 class="mt-5 mb-5">Quel projet souhaiterez vous realiser ?</h1></div>
			<div class="card-body">
				<!-- <h4 class="card-title">Secondary card title</h4> -->
				<p class="card-text mt-3">Décrivez en quelques lignes vos travaux à réaliser et entrez en contact avec des artisans qualifiés près de chez vous.</p>
				<form action="create" method = 'POST'>
					<div class="row ">
						<div class="col mt-3 m-0 p-0">
							{# <label for="codePostal" class="form-label mt-4">code postal</label> #}
                            {{form_row(form.codePostal)}}
                            {{form_errors(form)}}
							{# <input type="text" class="form-control" name="cp" id="" placeholder="code postal"> #}
						</div>
						<div class="col mt-3 ms-2 p-0">
							{# <label for="ville" class="form-label mt-4">Ville</label> #}
							{{form_row(form.ville)}}
                            {{form_errors(form)}}
							{# <?php echo form_error('ville'); ?> #}
						</div>
						
					</div>
			</div>
					<div class="form-group mt-3">
						{# <label for="titre" class="form-label mt-4">Titre</label> #}
						{{form_row(form.titre)}}
						{{form_errors(form)}}
					</div>	
					<div class="form-group mt-3">
						{# <label for="description" class="form-label mt-4">description</label> #}
						{# {{form_row(form)}} #}
						{{form_row(form.description)}}
						{{form_errors(form)}}
						{# <?php echo form_error('description'); ?> #}
					</div>
					<div class="mt-3">
						<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
					</div>
				</form>
			</div>
		</div>


</div>
